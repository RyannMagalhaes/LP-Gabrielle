---
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---

<span
  class:list={[
    'inline-block font-display text-small font-medium uppercase tracking-[0.14em] text-rose-deep',
    className,
  ]}
>
  <slot />
</span>
